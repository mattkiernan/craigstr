<li>
  <%= link_to category.name, [category.region, category] %>
  <% if current_user.admin? %>
    <%= link_to "Edit", edit_region_category_path(region, category) %> -
    <%= button_to "Delete", [category.region, category], method: :delete %>
  <% end %>
</li>
